# UI design tokens

`design-tokens.v1.json` is the typed semantic token source for the React/MUI
surface. Its CSS projection is linked into the Blazor shell. Both surfaces use
the same semantic names and light/dark values.

`shell-interop.v1.schema.json` specifies the versioned, opaque-token shell
context passed to the analytics microfrontend.
