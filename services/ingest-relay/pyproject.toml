[build-system]
requires = ["setuptools==83.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "novasteel-ingest-relay"
version = "0.1.0"
description = "Canonical event validation and Eventstream relay interface"
requires-python = ">=3.13"

[tool.setuptools.packages.find]
where = ["src"]
