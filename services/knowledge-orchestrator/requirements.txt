# Knowledge orchestrator dependencies.
# Core service uses only the standard library for offline mode;
# azure-monitor-opentelemetry enables optional observability.
azure-monitor-opentelemetry==1.8.9
